# Go RNN — Реализация рекуррентной нейронной сети на Go

Этот проект представляет собой **с нуля реализованную рекуррентную нейронную сеть (RNN)** на языке **Go**, без использования сторонних ML-библиотек.
Модель обучается на текстовых последовательностях, строит словарь, преобразует слова в числовые представления и выполняет проходы через RNN.

Цель проекта — показать, как работает RNN на низком уровне: матричные операции, скрытые состояния, прямой и обратный проход (BPTT), one-hot кодирование текста.

---

# 📁 Структура проекта

```
├── cmd
│   └── main.go          — запуск приложения
├── go.mod
├── internal
│   ├── rnn
│   │   └── rnn.go        — реализация RNN + функции активации
│   └── textutils
│       └── text_utils.go — словарь, токенизация, индексация
└── README.md
```

> Папка **activation** была удалена: все функции активации и их производные перенесены в `rnn.go`.

---

# 🚀 Возможности

В проекте реализованы:

### ✔ Рекуррентная нейронная сеть (RNN)

* прямой проход (forward)
* параллельный forward с использованием `goroutines`
* backpropagation through time (BPTT)
* обучение на последовательностях

### ✔ Текстовая обработка

* построение словаря (`BuildVocab`)
* one-hot кодирование входных слов
* преобразование текста в последовательность индексов

### ✔ Функции активации

Встроены прямо в `rnn.go`:

* `tanh` для скрытого слоя
* `softmax` или линейная активация на выходе (в зависимости от реализации)

---

# 📦 Установка

1. Установите Go ([https://golang.org/dl/](https://golang.org/dl/))
2. Клонируйте репозиторий:

```bash
git clone https://github.com/your-username/go-rnn-neuro.git
cd go-rnn-neuro
```

3. Обновите зависимости:

```bash
go mod tidy
```

---

# ▶ Использование

## Запуск приложения

```bash
go run ./cmd/main.go
```

Программа выполняет:

1. построение словаря из текстов
2. преобразование текста в индексы
3. обучение RNN
4. вывод результатов прямого прохода

---

# ✏ Пример данных

```go
texts := []string{
    "i am learning go",
    "go is awesome",
    "i love programming in go",
}
```

---

# 🧩 Примеры

## 🔹 Пример: преобразование текста в индексы

```go
text := "i am learning go"

vocab := map[string]int{
    "i": 0,
    "am": 1,
    "learning": 2,
    "go": 3,
}

indices := TextToIndices(text, vocab)
fmt.Println(indices)  // [0 1 2 3]
```

---

## 🔹 Пример: прямой проход через RNN

```go
inputText := "i am"
indices := TextToIndices(inputText, vocab)
hPrev := make([]float64, hiddenDim)

for _, index := range indices {
    x := make([]float64, inputDim)
    x[index] = 1.0

    output, hNew := rnn.Forward(x, hPrev)
    fmt.Println("Output:", output)

    hPrev = hNew
}
```

---

# 🔧 Основные функции

### `BuildVocab(texts []string) map[string]int`

Создаёт словарь word → id.

### `TextToIndices(text string, vocab map[string]int) []int`

Преобразует текст в последовательность индексов.

### `Forward(x, hPrev)`

Один шаг RNN:
возвращает `(выход, новое скрытое состояние)`.

### `Train(inputs, targets, epochs, lr)`

Обучение модели методом BPTT.

---

# 📘 Требования

* Go **1.16+**
  (желательно новее — проект использует `sync.WaitGroup`, горутины и slices)
