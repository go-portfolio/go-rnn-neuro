
# 📚 Теория RNN (Рекуррентных нейронных сетей)

Рекуррентные нейронные сети — это класс нейронных сетей, предназначенных для работы с **последовательностями**: текстом, аудио, временными рядами, данными датчиков.

В отличие от обычных (feedforward) сетей, RNN имеют **память**, то есть они *передают информацию во времени*.

---

## 🧠 1. Идея RNN

Обычная нейросеть принимает вход **один раз**, выдаёт результат и забывает всё.

RNN работает иначе:

```
xₜ → [RNN] → yₜ
         ↑
       hₜ₋₁
```

Каждый шаг RNN использует:

* текущий вход: **xₜ**
* скрытое состояние из прошлого: **hₜ₋₁**

и вычисляет новое скрытое состояние:

```
hₜ = tanh(Wₓ·xₜ + Wₕ·hₜ₋₁ + bₕ)
```

Таким образом RNN может "помнить" контекст, например:

* какое было предыдущее слово,
* какая часть предложения сейчас обрабатывается,
* какой временной шаг был раньше.

---

## ✏ 2. Формулы прямого прохода

Пусть:

* `xₜ` — входной вектор (one-hot)
* `hₜ` — вектор скрытого состояния
* `yₜ` — выход сети

Тогда:

### 2.1. Скрытое состояние:

```
hₜ = tanh( Wₓ xₜ + Wₕ hₜ₋₁ + bₕ )
```

Где:

* `Wₓ` — матрица весов вход → скрытый слой
* `Wₕ` — матрица весов скрытый → скрытый (рекуррентные связи)
* `bₕ` — смещение (bias)

### 2.2. Выходной слой:

```
yₜ = softmax( Wᵧ hₜ + bᵧ )
```

Если softmax не используется — выход может быть линейным.

---

## 🔁 3. Передача скрытого состояния

После каждого шага:

```
hₜ → hₜ₊₁
```

Состояние переносится далее.
Это позволяет сети накапливать знания о последовательности.

Пример:
Если модель читает предложение:

```
i love programming in go
```

то на слове `programming` сеть уже знает:

* что ранее было `i love`
* что речь идёт о человеке
* контекст предложения положительный

---

## 🏋️ 4. Обучение RNN: Backpropagation Through Time (BPTT)

Чтобы обучить сеть, нужно вычислить градиенты *не только по текущему шагу*, но и по предыдущим.

Это разворачивает RNN во времени:

```
x1 → h1 → y1
      ↓
x2 → h2 → y2
      ↓
x3 → h3 → y3
```

И обучение идёт так же, как в обычной нейросети — только через несколько копий сети.

Градиенты распространяются назад:

```
dL/dh₃ → dL/dh₂ → dL/dh₁
```

Проблема: **затухающие и взрывающиеся градиенты**
(поэтому появились LSTM и GRU — улучшенные RNN).

---

## 🔥 5. Почему RNN может обучаться на тексте

Текст → индексы → one-hot → RNN → предсказание следующего слова.

Пример:

```
"i am learning" → сеть → предсказывает → "go"
```

RNN учит распределение слов:

* после "i am" часто встречается глагол
* после "learning" часто идёт объект
* после "in" часто идёт язык программирования → "go"

---

## 🧩 6. Где в проекте реализована теория

### 6.1. Прямой проход

В функции:

```go
forwardParallel()
```

и в:

```go
computeHiddenLayer()
computeHiddenNeuron()
```

### 6.2. Словарь и one-hot

Функции:

* `BuildVocab`
* `TextToIndices`
* `PrepareData`

### 6.3. Активации

Реализованы внутри `rnn.go`
(папка activation удалена)

---

## 📌 7. Итог: как работает RNN в этом проекте

1. Текст превращается в числа.
2. Числа превращаются в one-hot вектора.
3. Сеть пошагово читает последовательность.
4. На каждом шаге обновляется скрытое состояние.
5. Выход сети пытается предсказать следующее слово.
6. Градиенты распространяются через всю последовательность (BPTT).
7. Веса обновляются, и сеть учится.

